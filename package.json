{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo run build",
    "build:api": "dotenv -- turbo run build --filter=api",
    "clean": "dotenv -- turbo run clean",
    "dev": "dotenv -- turbo run dev --no-cache  --continue",
    "dev:api": "dotenv -- turbo run dev --no-cache  --continue --filter=api",
    "start:api": "dotenv -- turbo run start --filter=api",
    "db:push": "dotenv -- turbo run db:push",
    "seed-db": "dotenv -- turbo run seed-db",
    "lint": "dotenv -- turbo run lint",
    "test": "dotenv -- turbo run test"
  },
  "devDependencies": {
    "dotenv-mono": "^1.3.10",
    "prettier": "^2.7.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@7.15.0",
  "dependencies": {
    "dotenv-cli": "^7.1.0"
  }
}
